---
apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  containers:
    - name: mycont
      image: sudharsan06122000/flashman:flashman1.0
      ports:
        - containerPort: 80
      envFrom:
        - secretRef:
            name: mysecret

  imagePullSecrets:
    - name: regcred
